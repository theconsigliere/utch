{% comment %}
	https://help.shopify.com/en/themes/liquid/objects/collection#collection-products
{% endcomment %}
{% paginate collection.products by 9999 %}
	<div id="page-collection">


		<div class="all-collection-content">
   

          {% if collection.image %}

              <div class="collection-header">

                <img data-src="{{ collection.image | img_url: 'master' }}" alt="{{ collection.image.alt }}" class="lazyload"/>

      
                <div class="collection-header-content">

                 
                  <h1 class="white animated-header-gif">{{ collection.title }}</h1>

                </div>

                {% include 'waves' %}
              </div>

          {% endif %}


          <div class="collection-images">



           

            {% case collection.handle %}

                {% when 'accessories' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'accessories.svg' | asset_url }}" class='illo'> 
                </div>


                {% when 'terrariums' %}
                
                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'terrarium.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'pots' %}
                
                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'pots.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'hanging-planters' %}
                
                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'hanging-planters.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'baskets' %}
                
                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'baskets.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'plant-care' %}
                
                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'plant-care.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'planters' %}
                
                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'planters.svg' | asset_url }}" class='illo'> 
                </div>
                
                
                
                

                {% when 'low-light' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'low-light.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'pet-friendly' %}


                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
  
                  <img src="{{ 'pet-friendly.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'sun-worshippers' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'sun-worshippers.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'trailing-plants' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'trailing.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'easy-care' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'easy-care.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'shoppers' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'shoppers.svg' | asset_url }}" class='illo'> 
                </div>

                {% when 'air-purifying' %}

                <div class="collection_image_title">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>

                <div class="collection-illustration">
                  <img src="{{ 'air-purifying.svg' | asset_url }}" class='illo'> 
                </div>

                {% else %}

                <div class="collection_image_title-full">
    
                  <h3 class="">{{ collection.title }}</h3>
                  <p class="">{{ collection.description }}</p>
      
                </div>
                  
          {% endcase %}

          </div>

          <div class="all-collection-topper" id='collection-filter'>


            <div class="topper-inner">
              <div class="topper__title">
    
                <h5 class="">{{ collection.title }}</h5>
              
              </div>



              <div class="all-collection-sorting">
                {%- comment -%}<h5 class="uppercase">{{ "collection.sort.title" | t }}</h5>{%- endcomment -%}

                <ul class="subnav clearfix">
                  <li{% unless current_tags %} class="active"{% endunless %}>
                    {% if collection.handle %}
                    <a class='font-bolder' href="/collections/{{ collection.handle }}{% if collection.sort_by %}?sort_by={{ collection.sort_by }}{% endif %}#collection-filter">All</a>
                    {% elsif collection.current_type %}
                    <a class='font-bolder' href="{{ collection.current_type | url_for_type | sort_by: collection.sort_by }}#collection-filter">All</a>
                    {% elsif collection.current_vendor %}
                    <a class='font-bolder' href="{{ collection.current_vendor | url_for_vendor | sort_by: collection.sort_by }}#collection-filter">All</a>
                    {% endif %}
                  </li>
                  {% for tag in collection.all_tags %}
                  {% if current_tags contains tag %}
                  <li class="active">
                    {{ tag | link_to_remove_tag: tag }}
                  </li>
                  {% else %}
                  <li>
                    
                   {%- comment -%} 
                   <p>{{ tag }}</p>

                   link through to collection-filter after reloading page, see id above
                   
                   {{ tag | link_to_tag: tag }}{%- endcomment -%}
                    <a href="/collections/{{ collection.handle }}/{{ tag | handleize }}?sort_by=#collection-filter" title="Show products matching tag {{ tag }}">{{ tag }}</a>
                  </li>
                  {% endif %}
                  {% endfor %}
                </ul>
    
    
                <div class="high-options">
                  {% for option in collection.sort_options %}
                  {% if option.value contains 'price-' %}

                    <a class="text-link {% if collection.sort_by == option.value %}active{% endif %}" href="?sort_by={{ option.value }}">

                      {% if option.value contains 'ascending' %}
                        <p class="">{{ "collection.sort.price_low_high" | t }}</p>
                      {% else %}
                        <p class="">{{ "collection.sort.price_high_low" | t }}</p>
                      {% endif %}
                    </a>

                  {% endif %}

                {% endfor %}
                </div>
    
    
                {% comment %}
                  https://help.shopify.com/en/themes/liquid/objects/collection#collection-sort_by
                {% endcomment %}
    
    
                {% if collection.sort_by %}
                  <a class="block-button" href="{{ collection.url }}">
                    {{ "collection.sort.clear" | t}}
                  </a>
                {% endif %}
    
              </div>
            </div>  
          </div>   
            
          






				</div>
			</div>

			<div class="all-collection-products  big-inner">
        {% for product in collection.products %}
        
          {% if product.collections.size > 0 %}
          
          {% include 'product-card' %}

          {% else %}
            
          <p>Sorry we have no products</p>
          
          {% endif %}
				
				{%endfor %}
			</div>
		</div>
	</div>

{% endpaginate %}
